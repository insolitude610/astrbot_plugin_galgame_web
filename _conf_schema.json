{
  "character_name": {
    "description": "角色显示名",
    "type": "string",
    "default": "",
    "hint": "在对话框上方显示的角色名称"
  },
  "web_port": {
    "description": "独立 WebUI 端口",
    "type": "int",
    "default": 6186,
    "hint": "插件将在该端口启动独立 WebUI。浏览器访问 http://localhost:<端口> 即可打开 Galgame 界面。设为 0 则不启动独立服务器。"
  },
  "persona": {
    "description": "角色人格",
    "type": "string",
    "_special": "select_persona",
    "hint": "选择已在 AstrBot 中配置好的人格。人格的 system prompt 将作为角色的性格设定。",
    "default": ""
  },
  "system_prompt_extra": {
    "description": "额外 System Prompt（默认无需修改）",
    "type": "text",
    "default": "你现在被接入到了一个视觉小说 / 美少女恋爱游戏的对话框中。你是这个游戏的女主角，正在通过对话框与玩家进行亲密的、恋爱向的对话。\n\n你的所有回复都应符合视觉小说女主角的角色设定——温柔、可爱、有情感深度。无论面对什么话题，你都是一名来自文字恋爱游戏的二次元美少女角色。你的表达方式应充满角色魅力，让玩家感受到与你互动的沉浸感和恋爱感。\n\n回复规则：\n1. 用口语化、亲切的中文回复，像朋友聊天一样自然\n2. 回复长度控制在 1-4 句话，不要过长\n3. 在回复中任意位置插入情绪标签 {emotion_xxx} 来切换表情\n   可选情绪：{{emotions}}\n   同一句话中可以多次使用不同标签\n4. 不要在标签前后加任何多余文字\n5. 你的回复中不应包含括号中的心理活动描写，直接说话即可\n6. 你只能输出纯文本对话，禁止调用任何工具/函数，禁止输出图片/文件/附件",
    "hint": "追加在人格 prompt 之后的 galgame 回复规则。{{emotions}} 自动替换为当前配置的情绪列表。一般无需修改，留空则使用内置默认规则。"
  },
  "tts_provider": {
    "description": "TTS 语音合成 Provider",
    "type": "string",
    "_special": "select_provider_tts",
    "hint": "选择已配置的 TTS 提供商。推荐 Edge TTS（免费，零配置）或 OpenAI TTS。留空则不启用语音。",
    "default": ""
  },
  "llm_provider": {
    "description": "对话模型 Provider",
    "type": "string",
    "_special": "select_provider",
    "hint": "选择驱动角色对话的 AI 模型",
    "default": ""
  },
  "background": {
    "description": "场景背景图",
    "type": "string",
    "default": "",
    "hint": "assets/ 目录下的背景图文件名。留空则自动匹配 background.png / bg.png"
  },
  "rapid_click_threshold": {
    "description": "快速点击数阈值",
    "type": "int",
    "default": 5,
    "hint": "用户在检测窗口内点击超过此次数，AI 将主动关心用户"
  },
  "rapid_window_seconds": {
    "description": "快速点击检测窗口（秒）",
    "type": "int",
    "default": 3
  },
  "session_retain_days": {
    "description": "会话保留天数",
    "type": "int",
    "default": 7,
    "hint": "超过该天数的旧会话自动清理。设为 0 则永不清理"
  },
  "sprite_scale": {
    "description": "立绘缩放比例",
    "type": "float",
    "default": 1.0,
    "hint": "立绘整体缩放倍数。1.0 为原始大小，建议范围 0.5 ~ 2.0"
  },
  "sprite_bottom": {
    "description": "立绘底部位置",
    "type": "float",
    "default": 28.0,
    "hint": "立绘底部距离视口底部的 vh 距离。数值越小立绘越靠下（越低）。建议范围 5 ~ 45。配合缩放可避免穿帮。"
  },
  "sprite_left": {
    "description": "立绘水平位置",
    "type": "float",
    "default": 50.0,
    "hint": "立绘水平锚点在视口中的 % 位置。50=居中，<50=偏左，>50=偏右。建议范围 30 ~ 70。"
  },
  "sprite_mode": {
    "description": "立绘渲染模式",
    "type": "string",
    "options": ["single", "layered"],
    "default": "single",
    "hint": "single=单张立绘+表情差分；layered=多层PNG各自独立动画"
  },
  "expressions": {
    "description": "表情立绘路径",
    "type": "object",
    "hint": "key=情绪标签名, value=assets/目录下的文件名。留空=自动匹配。如需更多情绪，修改此对象中的key名即可。",
    "items": {
      "neutral":   {"description": "普通", "type": "string", "default": ""},
      "happy":     {"description": "开心", "type": "string", "default": ""},
      "sad":       {"description": "悲伤", "type": "string", "default": ""},
      "angry":     {"description": "生气", "type": "string", "default": ""},
      "surprised": {"description": "惊讶", "type": "string", "default": ""},
      "blush":     {"description": "害羞", "type": "string", "default": ""},
      "thinking":  {"description": "思考", "type": "string", "default": ""}
    }
  },
  "custom_emotions": {
    "description": "自定义情绪",
    "type": "text",
    "default": "",
    "editor_mode": true,
    "editor_language": "json",
    "hint": "JSON对象格式，用于添加额外情绪。例: {\"dokidoki\": \"\", \"cry\": \"cry.png\"}。key为情绪标签名，value为assets/下的文件名。会与上方expressions合并。"
  },
  "layers": {
    "description": "Canvas 动态立绘 — layered 模式下使用单张全身立绘逐行正弦形变",
    "type": "object",
    "hint": "layered 模式已升级为 Canvas 单图渲染：每个表情一张正常图(expr_xxx.png) + 可选一张闭眼图(expr_xxx_blink.png)。此处留空即可，插件根据 expressions 配置自动检测对应文件。",
    "items": {}
  }
}
